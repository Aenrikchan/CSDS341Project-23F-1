version: '1'
services:
  client:
    build: ./client
    ports:
      - 8000:8000
    depends_on:
      - mssql
  mssql:
    image: mcr.microsoft.com/mssql/server:2022-latest
    ports:
      - 1433:1433
    environment:
      - ACCEPT_EULA=y
      - MSSQL_SA_PASSWORD=yourStrong(!)Password

